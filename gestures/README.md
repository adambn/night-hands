# Gestures

One JSON file per named pose or sequence. Recorded by physically posing the arm,
not by typing numbers.

Planned: `high-five.json` · `wave.json` · `hug.json` · `fist.json` ·
`count-to-3.json` · `sleep.json`

**Jonathan names these.**
